#include<iostream>
#include<string>
#include "initiative.h"

namespace dicebox {
  // Asks for name, dice, bonus of characters and writes them into the
  // InitiativeTracker 
  int initInitiative(InitiativeTracker* t);
  void clearScreen();
}
